/* se utiliza import para importar una funcion de otro archivo el cual es el FROM
 despues se coloca el nombre del archivio*/

import { restar } from './utils.js';

console.log(restar(10, 5));  
console.log(restar(20, 7));  
console.log(restar(3, 9));   
